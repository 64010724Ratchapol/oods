#-------max-----#

def max(x):
    if len(x) == 1:
        return x[0]
    else :
        m = max(x[1:])
        if int(m) > int(x[0]):
            return m
        else :
            return x[0]
inp = input("Enter Input : ").split()
print("MAX :",max(inp))
